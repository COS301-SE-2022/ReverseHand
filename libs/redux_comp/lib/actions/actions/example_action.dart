// all actions should be in their own file with name ending in action

class ExampleAction {
  // example of how actions should be names, note how file name is the same
  final String data; // if action needs to pass on any data

  ExampleAction({required this.data});
}
